import classes from "./AddedLocations.module.css";

const AddedLocations = () => {
  return (
    <div className={classes.main}>
      <h4>My Added Locations</h4>
    </div>
  );
};

export default AddedLocations;
